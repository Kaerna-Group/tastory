# Журнал изменений

## 2026-09-03 — публикация раздела владельца

- Apps Script версия **6** опубликована в 12:06:00 UTC из `b85e01b`; исходники редактора и снимок версии совпадают, SHA-256 `42cbf1f849fe8ff3893483d38baa8aac887babb6255da120cce4b13df6b1d57a`. Manifest, URL и настройки доступа сохранены.
- [Внешние проверки API](evidence/2026-09-03-workspace-admin-release.json): health успешен, оба новых действия отклоняют неверный токен, запрос списка без токена отклонён. Первый прогон остановился на таймауте 60 секунд; повтор всех четырёх запросов успешен за 1.8–4.8 секунды каждый. Это проверка доступности и отказов, не подтверждение реального списка владельца.
- Для интерфейса нужно обновить опубликованный сайт, войти владельцем и открыть **Участники тетради** в настройках. Дополнительных запусков функций Google не требуется.

## 2026-09-03 — доступ через Sheets подтверждён, раздел владельца

- В 14:37:18 владелец включил `SHEETS_AUTH_CONFIG` через `activateStagingSheetsAuth`, затем подтвердил работу обоих аккаунтов. [Результат](evidence/2026-09-03-sheets-auth-activated.json), D-02b закрыт.
- Добавлен раздел **Участники тетради**: список с ролями и статусами, обновление по кнопке и проверка структуры шести таблиц. Он доступен владельцу; после выхода или отказа в доступе данные очищаются.
- `admin.users.list` и `admin.health` повторно проверяют актуального владельца и срок токена под общей блокировкой. Список ограничен выбранной книгой, Google sub и ID ресурсов не возвращаются. Проверка схемы использует план существующей миграции без записи.
- Добавлены проверки прав, изоляции книги, ошибок и отмены, сценарии собранного Apps Script и desktop/mobile UI. [Границы и инструкция](workspace-admin.md). Приглашения и изменения прав остаются следующим шагом вместе с журналом операций и аудитом.
- Локальный `npm run check` успешен: 277 тестов, coverage 97.11% строк и 91.54% ветвей, 100 файлов в проверке архитектуры, типы, сборки и бюджеты. Четыре новых браузерных сценария успешны, desktop/mobile снимки проверены. В Windows после тестов потребовалось завершить зависший временный preview на порту 4188; тестовый runner завершился с кодом 0.

## 2026-09-03 — опубликован сервер с переключением на Sheets

- Из `68e61d6` выпущена версия Apps Script **5** по прежнему URL `/exec`, 11:26:42 UTC. Код редактора и снимок версии совпадают, manifest сохранён; [данные проверки](evidence/2026-09-03-sheets-auth-release.json).
- Настоящие запросы: health успешен, внешний вызов `activateStagingSheetsAuth` отклонён с INVALID_REQUEST, неверный токен — с UNAUTHENTICATED. Это проверки доступности и отказов, а не подтверждение настоящего Sheets-входа.
- Владелец запускает `activateStagingSheetsAuth` один раз, затем проверяет доступ owner/viewer в Tastory. До этого переключение не считается завершённым. Новых ручных прогонов схемы, переноса и фото не требуется.

## 2026-09-03 — перенос подтверждён, подготовлен вход через таблицы

- Получены [настоящие результаты переноса и повтора](evidence/2026-09-03-users-import-applied.json): 2 пользователя, 1 книга, 2 членства/приглашения, owner/viewer, без необработанных строк. D-02a закрыт.
- Добавлены чтение и строгая проверка Users/Workspaces/WorkspaceMembers, сопоставление Google sub с внутренним UUID и проверка роли в выбранной книге на каждом запросе. Disabled/pending пользователь, отключённое членство и другая личность получают отказ. Повреждённые данные или ошибка Sheets не возвращают доступ через старый реестр.
- Подготовлена owner-only `activateStagingSheetsAuth`: сверка завершённого переноса, активного владельца и отсутствия действующих непринятых приглашений, затем атомарная настройка backend/workspace. До её запуска действует прежний способ входа. [Инструкция и границы](sheets-auth-staging.md).
- Формат auth/spike сохранён; проверка прав использует внутренний UUID, прежний wire ID обеспечивает совместимость существующих приватных материалов. Автоматическое принятие новых пользователей и интерфейс управления участниками остаются следующим шагом.
- Успешны 255 тестов, типы, lint, архитектура и обе сборки. Добавлены 31 тест и проверка собранного IIFE с настоящей тестовой RSA-подписью: импорт → переключение → owner/viewer → отзыв и отказ при недоступной таблице. Исправлена только тестовая имитация пропавшего листа, чтобы её восстановление не меняло порядок fixture Map. Coverage выбранных модулей: 96.96% строк, 91.74% ветвей.
- Серверная публикация и реальная активация отслеживаются отдельно. D-02b не считается подтверждённым реальным входом до результата владельца.

## 2026-09-03 — D-01 подтверждён; подготовлен перенос пользователей

- Владелец прислал реальные результаты `planStagingSchema`, `setupStagingSchema` и повторного запуска: версия 1, шесть `keep`, `already-applied`. [Отчёты сохранены](evidence/2026-09-03-core-schema-applied.json), D-01 закрыт.
- Начат первый шаг D-02: добавлен редакторский перенос текущих привязок и приглашений в Users, Workspaces, WorkspaceMembers и Invites. Генерируются внутренние UUID, сохраняются Google sub и роли, проверяется единственный вступивший владелец. Привязка без действующего приглашения не получает членство.
- Техническая запись в Meta фиксирует UUID, время, исходную ревизию и контрольную сумму реестра до первой записи. Повтор продолжает прерванный перенос либо возвращает already-applied без записи; изменённые источники/строки, дубликаты, формулы и несовместимые ревизии останавливают операцию. Исходные свойства входа сохраняются.
- Добавлены [инструкция владельца](users-import-staging.md), 28 тестов и собранный сценарий schema → users plan/apply/repeat с запретом HTTP-вызова. `npm run check` успешен: 224 теста, типы, lint, архитектура, обе сборки и бюджеты; coverage выбранных модулей — 97.16% строк, 91.73% ветвей.
- Функции загружены в рабочий редактор в 10:49:48 UTC, код проверен повторным чтением: SHA-256 `767b77b89e06dd8605cde54d51e3d58c00549f927fe953714e500d076fa26d68`. Manifest и опубликованный API версии 4 сохранены. Реальный перенос запускает владелец; подключение входа к Sheets, полный RBAC, аудит и admin-панель ещё впереди.

## 2026-09-03 — D-01: базовая схема и повторяемая миграция

- Реализованы декларации шести листов: Meta, SchemaMigrations, Users, Invites, Workspaces, WorkspaceMembers. Добавлены предварительный просмотр `planStagingSchema` и настройка `setupStagingSchema`, доступные только из редактора.
- Миграция проверяет все заголовки до первой записи, хранит контрольную сумму и результат, фиксирует версию последней, продолжает частично выполненную настройку. Существующие данные и действующий реестр входа сохраняются. [Схема, инструкция и границы](schema-migrations.md).
- Добавлены 34 теста: сохранность данных, повторы, сбой до/после каждой записи и flush, конфликты, повреждения схемы, формулы, блокировки и безопасные отчёты. Собранные функции проверяются отдельно в изолированном runtime, включая запрет HTTP-вызова.
- `npm run check` успешен: 196 тестов, форматирование, lint, архитектура, типы, обе сборки и bundle budgets. Новая логика включена в coverage gate: всего 97.59% строк и 92.59% ветвей выбранных модулей.
- В 10:20:45 UTC функции загружены в редактор рабочего Apps Script, код проверен повторным чтением, SHA-256 `a8cbe20a465834e1051bd7674c86fa0f6995767e016c9f52de50029b88ae76b4`. Manifest сохранён, опубликованный web app остаётся на версии 4. Миграция в Google ещё не выполнялась: её запускает владелец по инструкции.

### Публикация предыдущего шага

- Оформление и подтверждение входа читателя отправлены в main коммитом `12782fc`. [CI](https://github.com/Kaerna-Group/tastory/actions/runs/33742177036) и [Publish staging](https://github.com/Kaerna-Group/tastory/actions/runs/33742186114) завершились успешно.
- Локально перед публикацией прошли 162 теста и 16 браузерных сценариев: 6 общих smoke и 10 auth fixtures. Последующий D-01 меняет сервер и документацию; пользовательские экраны остались такими, как в этой публикации.

## 2026-09-03 — вход читателя восстановлен, публикация оформления

- Получен [успешный реальный вход читателя](evidence/2026-09-03-access-viewer-success.json), роль viewer, 3715 мс. Ошибка привязки больше не блокирует этот вход. Отзыв/восстановление отсутствуют в записи и остаются открытыми.
- В публикацию включены подготовленные логотип, иконки, слоган и метаданные карточки ссылки; файлы учитывают каталог сайта, адрес задаётся VITE_SITE_URL. Добавлена документация оформления.
- Владелец подтвердил работу и поручил начать следующий этап после отправки изменений: D-01, схема таблиц и миграции. Полный gate этапа 0 не объявляется пройденным, открытые условия сохраняются.

## 2026-09-03 — подготовлено исправление ошибочного объекта приглашения

- Владелец подтвердил: во второй объект `STAGING_AUTH_BINDINGS` попало приглашение читателя с `role`/`expiresAt`, вместо серверной привязки с `sub`/`joinedAt`.
- Подготовлена точечная правка: удалить только ошибочный объект, сохранив исходную привязку владельца и оба приглашения. На синтетических данных проверены схема, сохранность владельца, создание правильной привязки при допуске, отсутствие дубликатов и сохранность привязок при отзыве/восстановлении.
- Инструкция обновлена, Google ID из сообщения не копируется в файлы. Изменение в Google и успешный вход читателя ещё не подтверждены; код API не менялся.

## 2026-09-03 — обнаружена неверная запись привязки

- Реальная [диагностика владельца](evidence/2026-09-03-auth-bindings-diagnostic.json) локализовала нарушение схемы во втором объекте `STAGING_AUTH_BINDINGS`: `sub`, `joinedAt` и структура объекта. Приглашения, конфигурация клиента, ключи и блокировка прошли свои проверки.
- Подтверждено, что ошибка схемы привязок объясняет `AUTH_UNAVAILABLE`. Диагностика скрывает содержимое записи, поэтому конкретная правка требует её просмотра; одинаковые пути не позволяют отличить отсутствующие поля от неверных значений.
- Обновлены результат и следующий шаг: проверить только второй объект, сохранив owner и действующие привязки. Код и удалённые свойства не менялись, исправление и вход читателя ещё не подтверждены.

## 2026-09-03 — диагностика входа из редактора Apps Script

- Владелец подтвердил настройку двух приглашений, но [новый отчёт](evidence/2026-09-03-access-after-setup.json) снова содержит `AUTH_UNAVAILABLE`. Вход читателя и отзыв не считаются выполненными.
- Добавлена `diagnoseStagingAuth`: отдельный запуск из редактора проверяет формат приглашений/привязок, конфигурацию клиента, кэш и получение ключей Google, доступность блокировки. Возвращает только статусы, количества и безопасные названия полей; не выводит значения свойств, идентификаторы и тексты исключений, не изменяет свойства или кэш.
- Диагностика недоступна через публичный HTTP API. Подготовлены инструкция одного запуска и семь тестов на корректные/повреждённые данные, отказы сервисов, отсутствие утечек и изменений. `npm run check` успешен: 162 теста, форматирование, lint, архитектура, типы и сборки; дополнительно собранная глобальная функция проверена в изолированном Apps Script runtime, включая запрет HTTP-вызова.
- Функция загружена в редактор рабочего Apps Script и проверена повторным чтением исходного кода. Manifest сохранён, опубликованный API остаётся на версии 4; свойства и привязки не менялись. Для установления причины нужен результат запуска владельцем.

## 2026-09-03 — разбор ошибок входа читателя

- Принят как неуспешный [отчёт двух AUTH_UNAVAILABLE](evidence/2026-09-03-access-unavailable.json); вход читателя, отзыв и восстановление не зафиксированы.
- Предоставленный owner-only JSON прошёл настоящую схему приглашений. Подготовлен полный массив owner/viewer с неизменённой записью владельца; оба файла с адресами хранятся только в `.local`.
- Публичный health и отклонение синтетической неверной подписи проверены на staging API; это не проверка свойств после настоящего входа. Причина сбоя остаётся неизвестной. Подготовлена инструкция проверки через существующую `setupStagingAuth`; код, серверные свойства и привязки не менялись.

## 2026-09-03 — подготовлено приглашение тестового читателя

- Получен адрес второго аккаунта; готовая запись `viewer` и инструкция добавления, отзыва, восстановления и удаления временного доступа сохранены локально вне Git.
- JSON, роль и будущий срок первого входа проверены. В Google запись ещё не применена: редактор требует входа владельца, а текущее подключение clasp не меняет Script Properties. Остальные приглашения и привязки не изменялись.
- Уточнён следующий шаг в инструкции и плане; реальный прогон отзыва/восстановления остаётся открытым.

## 2026-09-03 — отказ при входе вторым аккаунтом подтверждён

- Проверен и сохранён [реальный отчёт отказа](evidence/2026-09-03-access-denied.json): после выхода запрос `auth.signIn` получил `ACCESS_DENIED` за 2921 мс, без аккаунта и роли в событии. Флаг `deniedSignIn: true` соответствует записи.
- Сценарий второго неприглашённого аккаунта определён по контексту передачи отчёта; сам файл не раскрывает выбранную почту и список приглашений. Эти границы зафиксированы в документации.
- Следующий шаг — временное приглашение `viewer` для второго аккаунта, отзыв и восстановление. Отчёт проверен по структуре, хронологии и допустимым полям; приложение и сервер не менялись.

## 2026-09-03 — повторный вход владельца подтверждён

- Проверен и сохранён [реальный отчёт повторного входа](evidence/2026-09-03-access-owner-repeat.json): вход, явный выход, второй вход той же личности с ролью `owner`, затем успешная проверка доступа. Флаг `repeatedSignIn: true` соответствует четырём событиям.
- Обновлены текущие статусы roadmap, checklist и инструкции. Первый частичный отчёт сохранён как история. Повторять принятый сценарий в этом браузере не требуется; отказ второму аккаунту, отзыв/восстановление и остальные условия этапа ещё открыты.
- Проверены структура, хронология, requestId и отсутствие персональных данных в экспортируемых полях. Изменены только документация и доказательство; код и сервер не менялись.

## 2026-09-03 — частичный отчёт основного аккаунта

- Проверен и сохранён [обезличенный отчёт владельца](evidence/2026-09-03-access-owner-partial.json): текущий доступ с ролью `owner` подтверждён за 3576 мс, затем выполнен явный выход.
- Повторного входа в записи нет; все четыре отметки сценариев остаются `false`. S0-04/S0-05 не закрыты. Отсутствие события не трактуется как ошибка Google-входа.
- Уточнена инструкция: после выхода снова войти тем же аккаунтом в той же вкладке, дождаться отметки повторного входа и только затем скачать файл. Код приложения и сервер не менялись.

## 2026-09-03 — отчёт для проверки приглашений

- В блоке аккаунта staging добавлен раскрывающийся раздел **Проверка приглашений**: запись повторного входа, отказа, отзыва и восстановления с экспортом JSON. В файл попадают только местные псевдонимы аккаунтов, роли, результаты, requestId и время; имена, почта, Google ID и токены исключены.
- Запись включается вручную, ограничена 30 событиями и сохраняется между явными выходами. Завершение/очистка/новый прогон исключают поздние ответы; сетевые ошибки и истечение токена не считаются отзывом. Клиент auth сохраняет requestId для отчёта; серверный контракт и правила приглашений не менялись.
- Подготовлена [инструкция реального прогона](google-access-staging.md), включая отдельного тестового читателя и восстановление его приглашения. Реальные проверки S0-04/S0-05 ещё ожидаются; фото и конкурентные записи остаются принятыми.
- Проверка `npm run check` успешна: 155 тестов, formatter/lint, архитектура, TypeScript, сборки и Apps Script smoke. Auth E2E: 10 успешных сценариев на desktop/mobile, в том числе скачивание и проверка состава отчёта. Начальный JS 83.1 KiB gzip, все chunks в бюджете.

## 2026-09-03 — реальный прогон одновременных записей принят

- Получены JSON и снимок владельца: восемь успешных шагов за 40.689 с. Две записи сохранены после разрешения конфликта; повторные операции не добавили дубликатов и не откатили новую версию.
- Структура и последовательность результатов сверены, обезличенный JSON сохранён в `docs/evidence/2026-09-03-concurrency-owner.json`. S0-08 и соответствующий S0-05 в backlog отмечены выполненными.
- Учтено пересечение по времени с замером публичного API. Общий gate остаётся открытым для оставшихся сценариев доступа и архитектурных решений; повторять принятую пробу сейчас не требуется.

## 2026-09-03 — исходный замер задержек API

- До начала задачи проверена публикация предыдущих изменений: GitHub main совпадал с `5513904`.
- Добавлена команда `npm run benchmark:staging`: последовательная серия health/echo, проверка общих контрактов, redirect, requestId и текста; размеры UTF-8, p50/p95, отдельные ошибки, ограничение числа запросов и остановка при недоступности.
- На опубликованной Apps Script version 4 выполнены 3 прогрева и 60 измерений. 59 измерений успешны, один echo завершился таймаутом 15004 мс. Неуспешный результат сохранён без автоматического повтора; [методика и исходные данные](staging-measurements.md).
- Добавлено 11 тестов инструмента. S0-09 отмечен частично выполненным; реальный Google-прогон конкурентных записей остаётся открытым. Фото не изменялись.
- Проверка `npm run check` успешна: 148 тестов, format/lint, архитектура, TypeScript, обе сборки и Apps Script smoke. Интерфейс и сервер не менялись; повторная публикация Pages/Apps Script для этой задачи не требуется.

## 2026-09-03 — фото приняты владельцем

- Владелец подтвердил, что фото готовы и работают корректно. Проба фото принята; дальнейшую работу над ней пользователь попросил не продолжать.
- Статусы roadmap/checklist обновлены. Следующий ожидаемый результат — прогон одновременных записей; подтверждение фото не закрывает остальные проверки платформы.

## 2026-09-03 — проба одновременных записей

- Сервер опубликован в Apps Script version 4 из кода `91ecc9f`. Успешны 7 live-проверок настроек/отказов; [отчёт](staging-verification.md#выпуск-пробы-одновременных-записей).

- Добавлен staging-блок «Одновременные изменения»: два параллельных запроса с одной ожидаемой версией, проверка конфликта, повторов без дубликатов и сохранения следующей версии.
- В отдельном служебном листе Google Sheets состояние и квитанции хранятся одной ячейкой под ScriptLock с flush до освобождения блокировки. Доступ ограничен текущим владельцем, объём — 100 пробами.
- Добавлены тесты конкурентной отправки в браузере, прав, изоляции, ошибок хранилища, лимита и повторных операций; результат можно скачать без токенов и персональных данных.
- [Инструкция владельца](google-concurrency-staging.md). Реальные S0-08 и загрузка фото пока не подтверждены; полный gate остаётся открытым.

## 2026-09-03 — первый вход подтверждён, подготовлена проба фото

- Серверная проба опубликована в Apps Script version 3 из кода `d7f9447`; успешны 7 live-проверок настроек/отказов. Настоящий Drive upload ещё не подтверждён; [отчёт](staging-verification.md#выпуск-пробы-приватного-фото).

- Владелец подтвердил настоящий вход Google и роль владельца. Повтор, отзыв и другие браузеры остаются отдельными проверками.
- Добавлен staging-блок «Пробное фото»: уменьшение JPEG/PNG/WebP в браузере, загрузка JPEG и миниатюры, отдельное чтение через защищённый API, восстановление после повторного входа и удаление тестовых файлов.
- Сервер проверяет `sub`, актуальную роль owner и приватность Drive. Ограничены размеры и число файлов; повтор загрузки не создаёт дубликаты. Сессия и фото остаются в памяти браузера.
- Добавлены проверки прав, изоляции аккаунтов, ограничений и частичных сбоев, а также браузерный сценарий с настоящим canvas. Реальный Drive-прогон пока ожидается: [инструкция](google-photos-staging.md).

## 2026-09-03 — auth API опубликован

- Владелец подтвердил запуск `setupStagingAuth`. Выпущен Apps Script deployment version 2 из кода `7300d52`, существующая ссылка /exec сохранена.
- Настоящий API сообщает `auth: staging`. Успешны 7 проверок настроек/отказов и echo; связь с API дополнительно подтверждена в интерфейсе HTTPS-сайта.
- Поддельная RSA-подпись с актуальным Google kid, неподписанный и повреждённый токены отклоняются. Запрос профиля без credential и вызов owner setup через HTTP запрещены.
- [Подробный результат](staging-verification.md#активация-auth-api). Положительный вход Google ещё ожидается; S0-04/S0-05 не закрыты.

## 2026-09-03 — OAuth client подключён к настройке

- Полученный от владельца публичный Client ID сохранён в локальном staging и GitHub variable `STAGING_GOOGLE_CLIENT_ID`.
- `setupStagingAuth` автоматически заполняет отсутствующую `GOOGLE_CLIENT_IDS` этим клиентом при запуске владельцем. Существующая allowlist сохраняется; HTTP-запрос не может включить настройку.
- Обновление загружено в рабочий Apps Script HEAD. Ещё нужны owner consent и выпуск новой версии /exec, затем настоящий вход с HTTPS-сайта.
- Проверка: полный `npm run check`, 107 тестов и IIFE smoke, включая автоматическую настройку клиента и отказ токену с прежним audience.

## 2026-09-03 — подготовка Google auth и приглашений

- Реализованы auth.signIn/auth.me, проверка Google RS256/claims, фиксированный endpoint и кеш публичных ключей, fail-closed ошибки без credentials.
- Под ScriptLock приглашение однократно связывается с Google sub; отзыв и смена роли действуют на следующий защищённый запрос. До 10 аккаунтов в временном реестре Script Properties, schemaVersion 0.
- В настройках добавлены вход Google, профиль, выход, повторная проверка доступа; токен только в памяти до expiry. При незаданном Client ID интерфейс явно сообщает о настройке.
- Добавлена owner-функция setupStagingAuth и [инструкция](google-auth-staging.md). Код загружен и сверен в рабочем Apps Script HEAD; опубликованный /exec пока остаётся на версии 1 до OAuth-настройки и consent владельца.
- Сохранён установленный в Google-проекте часовой пояс Europe/Minsk. Устранена несовместимость bigint-литерала с загрузчиком Apps Script; сборка теперь проверяет её по синтаксическому дереву.
- Принят ограниченный эксперимент [ADR 0004](adr/0004-staging-google-auth.md): RSA-библиотека + отдельная валидация Google claims, production-решение ещё не принято.
- Проверено: 107 unit/contract/architecture тестов; 6 browser smoke и 4 auth E2E с fixtures на desktop/mobile; сборки и изолированный IIFE с настоящими RSA-подписями. Начальный JS 83.1 KiB gzip. При установке зависимостей npm audit: 0 известных уязвимостей.
- Реальный вход, OAuth Client ID и запуск owner setup ещё ожидаются. S0-04/S0-05 и общий gate платформы остаются открытыми.

## 2026-09-02 — основа проекта

### Реализовано

- Инициализирован локальный Git (main), без remote и commit.
- Созданы пять npm workspaces, закреплены Node/npm и точные версии зависимостей.
- Настроены React/Vite/Tailwind/Router/Query, FSD и alias.
- Подготовлены библиотека, настройки, 404, светлая/темная тема с сохранением.
- Добавлены общие API v1 схемы, mock/HTTP transport, правило времени рецепта, дизайн-токены.
- Собирается Apps Script health/echo с глобальными entrypoints и изолированным smoke.
- Подготовлены env templates, clasp, CI, архитектурные проверки, coverage, bundle budgets и Playwright.
- Создана документация и первые три ADR.

### Проверка

- Unit/contract/architecture tests: **44 успешно**.
- Playwright: **6 успешно**, desktop и mobile Chromium.
- TypeScript и проверка архитектуры: успешно.
- Web и Apps Script build: успешно.
- Начальный маршрут: **83.1 KiB gzip** при бюджете 250 KiB; JS chunks меньше 200 KiB raw.
- npm audit при установке: **0 известных уязвимостей**.
- Чистая установка npm ci и единый npm run check:all: **успешно**.
- Coverage выбранного фундамента: **90.16% lines**, **94.59% branches**.
- Проверены ссылки всех 17 Markdown-документов README/docs.
- Локальный просмотр запущен на http://127.0.0.1:5178/; стандартная команда npm run dev использует 5173.
- Для E2E выделен порт 4187, переопределяется через PLAYWRIGHT_PORT.

### Следующие шаги

Подключить Google staging, выполнить [этап 0](spike-checklist.md). Авторизация, Sheets/Drive, CRUD, публикация приложения и PWA ещё не реализованы.

## 2026-09-02 — подключение GitHub

- Настроен origin `git@github.com-personal:Kaerna-Group/tastory.git`.
- Подтверждён доступ личного SSH-ключа аккаунта Ermolz69.
- В этом репозитории закреплён автор и создатель коммитов: Ermolz <00ermzahar@gmail.com>.
- Основа опубликована в main коммитом `3bd468b`; настроено отслеживание origin/main.
- GitHub связал автора и создателя коммита с аккаунтом Ermolz69.
- Первый [CI на Ubuntu](https://github.com/Kaerna-Group/tastory/actions/runs/33673983434) завершился успешно: Quality and smoke, обе сборки и браузерные проверки.
- Подготовлен [порядок следующих задач](next-steps.md): Google staging, gate платформы, данные и пользователи, затем структурированный рецепт.

## 2026-09-02 — подготовка Google staging

- Создан отдельный Google Apps Script project Tastory - Staging API и настроен локальный clasp.
- В Google загружены Code.js и manifest с health/echo и функцией `setupStaging`.
- Добавлено создание тестовых Spreadsheet и Drive folder с записью ID в Script Properties, блокировкой и повторным использованием сохранённых ресурсов.
- Настройка отказывает для другого окружения и ресурсов без метки staging; при ошибке создания папки сохраняет уже записанную привязку таблицы.
- `setupStaging` не добавлена в публичный API. Проверяется отказ для такого RPC action.
- Подготовлены [инструкция владельца](google-staging.md) и локальная карточка со ссылкой на Google-проект. Приватные ссылки и конфигурация clasp исключены из Git.
- Создание таблицы и папки **ещё не выполнено**: текущий доступ clasp не включает Drive/Sheets, требуется разрешение Google при запуске владельцем.
- Схема данных, web app deployment, auth и удалённый gate остаются открытыми.
- Проверка `npm run check` успешна: 54 теста, TypeScript, lint, архитектура, обе сборки и smoke собранной функции настройки. Исходники Code.js и manifest, прочитанные обратно через Google API, совпали с локальной сборкой.

## 2026-09-02 — подтверждение создания Google-ресурсов

- Владелец подтвердил, что `setupStaging` работает и ссылки на созданные таблицу и папку открываются. S0-01 закрыт по этому подтверждению; независимая проверка данных из API ещё не выполнена.
- Через Google API проверены deployments: имеется только HEAD без web app entrypoint. Опубликованного адреса `/exec` ещё нет.
- Обновлены статус staging, локальная карточка и пошаговая инструкция публикации диагностического health endpoint. Следующий шаг — получить URL web app и проверить настоящий запрос из Tastory.

## 2026-09-02 — подключение Tastory к Google API

- Присланный владельцем URL `/exec` сверён с Apps Script: web app Tastory API, deployment версии 1.
- Заполнен игнорируемый `apps/web/.env.staging.local`, Tastory запущен в staging на `http://127.0.0.1:5178`.
- GET/POST health возвращают HTTP 200 и JSON после redirect; POST сохраняет requestId. Публичный вызов `setupStaging` отклонён с `INVALID_REQUEST`.
- В браузере подтверждено «Соединение проверено» при работе настоящего HTTP transport. Сборка `npm run build:staging` успешна.
- Локальный manifest синхронизирован с уже выбранными владельцем настройками web app. Удалённый код не менялся.
- S0-02 закрыт, S0-03 частично выполнен. Подробности и ограничения зафиксированы в [записи проверки](staging-verification.md); auth, хранение рецептов и полный gate остаются впереди.

## 2026-09-02 — HTTPS staging и проверки браузеров

- Включён GitHub Pages с HTTPS и workflow-публикацией; [Tastory staging](https://kaerna-group.github.io/tastory/) опубликован из `0285460`.
- Добавлен ручной Publish staging: quality и smoke → сборка с Google API → публикация → реальные браузерные запросы. Google API URL задан repository variable, credentials не требуются.
- Локальный `npm run check` успешен: 54 теста, типы, lint, архитектура, обе сборки и размеры bundle. Перед публикацией в GitHub прошли также 6 browser smoke.
- [Первый запуск](https://github.com/Kaerna-Group/tastory/actions/runs/33680492260) успешен: health через интерфейс проходит в Chrome, Edge, Firefox и WebKit. Во встроенном браузере отдельно подтверждено соединение по HTTPS.
- Echo в этом прогоне вернул ACTION_DISABLED: 4 проверки пропущены. Установлено, что пользователь открыл отдельный привязанный к таблице проект со стартовой функцией; рабочий backend и код сохранны в Tastory - Staging API.
- S0-03 остаётся частичным: нужны включение echo в рабочем проекте, успешный повтор и настоящий Safari. Далее — Google-вход и приглашения.

### Завершение транспортных проверок

- Владелец включил echo в рабочем Tastory - Staging API; опубликованный backend подтвердил точный возврат тестовой строки.
- [Обязательный повтор](https://github.com/Kaerna-Group/tastory/actions/runs/33681506028) с `require_echo=true`: **8 успешно, без пропусков и нестабильных повторов** — Chrome, Edge, Firefox и WebKit.
- Добавлен и успешно выполнен [Verify Safari staging](https://github.com/Kaerna-Group/tastory/actions/runs/33681876348): настоящий Safari 26.5.2 на macOS 26.5.2, **3 проверки успешно** (интерфейс, health, echo). Использован нативный SafariDriver без дополнительных зависимостей.
- S0-03 закрыт; следующий приоритет — S0-04/S0-05: Google Sign-In, проверка токена и допуска по приглашению. Полный gate платформы остаётся открытым.
